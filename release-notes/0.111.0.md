# 0.111.0 — Create folders from the folder panel

Add a new Stashpad folder without leaving the folder panel. A **"+" button** in the
Folders heading opens a quick name prompt; the folder is created (with intermediates),
seeded with a Home note, and opened — the same result as creating one from Settings,
now one click away.
