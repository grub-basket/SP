# 0.208.1

Corrections to the 0.208.0 onboarding work, found by re-reading the change and
then driving it in a real vault rather than trusting the build.

- **The empty state's demo button seeded the wrong folder.** It created a
  separate folder instead of filling the one you were looking at.
- The generated Home note's filename now matches the naming used everywhere
  else.
