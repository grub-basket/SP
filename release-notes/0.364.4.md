# 0.364.4

A store-readiness cleanup — no user-facing behavior changes.

## Store-lint blockers cleared

The community-store lint reported 13 errors that a store review would reject. All are now resolved; the lint reports 0 errors.

- **Inline style writes replaced with classes** (`no-static-styles-assignment`): the mobile autocomplete popup's hold-until-the-keyboard-settles now toggles an `is-awaiting-reveal` class instead of writing `style.visibility`, and the composer-debug modal's trace box and Copy button use `.stashpad-debug-box` / `.stashpad-debug-copy` classes instead of inline width, font, and margin writes.
- **`console.log` / `console.info` → `console.debug`**: the performance-profile report (still copied to the clipboard), the stale archive-folder prune, the legacy render-cache removal and IndexedDB migration, the settings split, and the structure-snapshot retention notice.
- **Manual heading removed** (`settings-tab/no-manual-html-headings`): the "Composer command button" settings section uses a proper `Setting` heading instead of an `h4`.
