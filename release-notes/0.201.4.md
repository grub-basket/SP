# 0.201.4

- **The focused-note header shows the task checkbox.** Drilling into a task
  hid its completion state — the header now renders the same toggleable
  checkbox as a list row (in both the full header and the sticky mini header
  that appears when you scroll), so a parent task can be checked off without
  climbing back out.
