# 0.272.3

## Links rail splits by type when it gets long

When a note has many links, the outgoing links and the backlinks now split onto
their own rows, one kind per line, instead of sharing one long row. A short list
stays on a single line.
