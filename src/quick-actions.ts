/** 0.320.0: superseded by note-actions.ts (the unified catalog). Kept as a
 *  re-export so older import paths keep resolving. */
export { QUICK_ACTION_CATALOG, QUICK_MENU_MORE, type QuickActionDef } from "./note-actions";
